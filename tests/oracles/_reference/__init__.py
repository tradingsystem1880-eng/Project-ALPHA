"""Test-only reference implementations, transcribed independently from primary sources.

These are deliberately naive, loop-based, and written WITHOUT reading the production module —
their value is independence, not speed. Never import them outside ``tests/oracles``.
"""
