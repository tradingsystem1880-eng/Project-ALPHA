---
paths:
  - "apps/alpha-web/**"
---
# alpha_web + frontend rules

Verbatim relocation from the pre-v2 CLAUDE.md MODULE MAP (drift-tested against `tests/fixtures/claude_md_v1.md`). The core `CLAUDE.md` DAG paragraph and golden rules still apply here.
### `alpha_web` (`apps/alpha-web/src/alpha_web/`) — the **ALPHA Workstation**: a thin JSON+SSE backend serving a built SPA (top of DAG; subprocesses CLI actions and status projections, composes nothing). Launch: `uv run alpha-web` → http://localhost:8801 (the socket remains loopback-only; the loopback-IP URL redirects to the canonical WebAuthn origin). Its platform imports are `alpha_core.config` plus supported public CLI seams for catalog/run-store, artifact contracts/projections, job capacity/durable leases, owner-auth verification, and the paper journal; bounded artifact reads may use Polars. Provider/system, control-plane, engine-backed, Qlib, and Kronos work stays behind CLI/out-of-process boundaries. The web process never imports or executes the engine, gauntlet, Nautilus, Qlib, or Kronos in-process.
| Module | Responsibility | Key public symbols |
|---|---|---|
| `app.py` | FastAPI factory: mount `/api` routers, serve the SPA (catch-all), `/healthz`, `main()` (uvicorn) | `create_app`, `main` |
| `api/` | Thin JSON routers | existing run/job/activity/catalog/control/paper/candle/analytics/workspace routes plus `development.py` (projects, immutable setup, suites, holdouts, jobs, evidence, AgentBrief), `ml.py` (worker readiness, exchanges, durable jobs, evaluations, tear sheets), and bounded Research Case/literature/owner-action routes |
| S5a3 extension | Adds the bounded semantic-projection GET to the Research Case read plane | `GET /api/research/cases/{project_id}/semantic-projection` |
| S5b owner-auth extension | The existing owner-auth challenge/perform routes accept the one closed `record_semantic_event` literal and special-dispatch through `alpha_cli.owner_auth`'s atomic receipt/event seam. It never calls `_action_argv` or `_run_json` for that action; there is no new route, screen, frontend-derived authority, or public semantic presentation in S5b | `POST /api/owner-auth/actions/challenge`; `POST /api/owner-auth/actions/perform` |
| S5c/S6 read-plane extension | Existing Research Case responses add strict `ResearchStudyStatusV1`; the existing Study tab fetches the unchanged semantic GET and renders only its server-masked points plus the verified Touch-ID event cycle, active contract, existing D1 attempts/budgets, promotion reference/readiness, and next-owner-action guidance. The projection has `authority: none`; D1 launch remains owner-CLI-only and no new route, screen, owner mutation, MCP tool, D2, promotion, paper, broker, or order control is added | `ResearchCase.study_status`; `GET /api/research/cases/{project_id}/semantic-projection` |
| Project workspace projection | The existing Development Center reads strict `StrategyProjectWorkspaceV1` through `alpha project workspace show` and may invoke only the bounded generated-reference refresh through `sync`. The backend remains a subprocess-only relay; it never opens SQLite, repairs generated bytes, copies artifacts, or grants research-gate, promotion, sandbox, paper, broker, or order authority | `GET /api/projects/{project_id}/workspace`; `POST /api/projects/{project_id}/workspace/refresh` |
| `api/models.py` | Strict stable JSON response contracts; OpenAPI source | Pydantic models including `ChartBundle`, native tear sheets, project lineage, stage/jobs, evidence, ML exchange/evaluation, and bounded Research Case contracts |
| `run_authority.py` | One fail-closed resolver for empirical Workstation launch context — every empirical child process gets its authority decided here, and a missing or unverifiable context is a denial, never a default | `RunContextDenied`, `resolve_run_context` |
| `_invoke.py` | Background job runner: spawn `alpha` in its own process group, guard lease/pump initialization, keep descendant groups leased after leader exit, poll audited cancellation, tail stdout, parse run/session ids, publish SSE with `Last-Event-ID` replay, and project exact elapsed/current activity plus explicitly evidence-bounded same-session ETA | `Job`, `launch`, `event_stream`, `list_jobs`, `cancel_job`, `JOBS`, `RUN_TYPE` |
| `_activity.py` | **Live desk**: per-connection polling SSE diff of the run store + job registry (stat-only mtime scan; manifest read only on change) — runs launched ANYWHERE (UI/CLI/MCP) surface live | `activity_events`, `snapshot_runs`, `job_states`, `clamp_poll` |
| `_runs.py` | Bounded filesystem reads over the run store (incl. native analytics and legacy projections) | `query_runs`, `run_detail`, readable-completion checks, equity/trades/forecast/null/trial/propfirm/origin projections |
| `_development.py` · `_ml.py` | Thin subprocess adapters for CLI-owned development/evidence state and isolated ML exchange/jobs | project/suite/evidence projections; worker status/launch/import/evaluation projections; direct and governed-suite ML job-lineage reconciliation |
| `_development.py` project-workspace extension | Thin subprocess relay for the verified generated workspace read and bounded refresh; never opens SQLite or repairs bytes | `project_workspace`, `refresh_project_workspace` |
| `_catalog.py` · `_candles.py` | Subprocess `alpha … --json` (strategy/command/symbol/provider/system projections; PIT candles cached on parquet mtime) | `_run_json`; `strategies`, `commands`, `symbols`, `providers`, `system`; `candles` |
| `_options.py` · `_risk.py` · `_screener.py` · `_research.py` | Subprocess the matching `alpha` sub-command → `--json` for the SPA panels | `greeks`/`iv`/`curve`; `scenario`; `quote`/`news`; `compare` plus bounded Research Case capture/get/propose/pilot/status/report |
| S5a3 extension | `_research.semantic_projection` invokes only the closed CLI vocabulary | `alpha research semantic-projection PROJECT_ID --json` |
| S5b owner-auth extension | `perform_action` returns the already committed or read-only recovered semantic authorization through the existing response shape; all other owner actions retain their existing CLI dispatch | no subprocess/CLI/job seam for `record_semantic_event` |
| `_figures.py` · `api/figures.py` | Serve rendered figures **without matplotlib in the web process**: derive the cache key from `alpha_cli.figure_cache` plus a key environment the CLI publishes, serve on hit, subprocess `alpha figures render` on miss behind a single-flight lock. Bare `…/image` gets `ETag` + `must-revalidate`; `?key=` gets `immutable`; a stale key 409s with the current one rather than silently returning different bytes at one URL. It must never import `alpha_research` — a DAG contract enforces this | `key_environment`, `figure`, `metadata`; three bounded endpoints |
| `_workspaces.py` | Named research-context store (`data_dir/web/workspaces/<slug>.json`; traversal-guarded). A workspace is the symbol/window/project/snapshot you were working in, not a window arrangement; legacy documents keep an ignored `dockview` blob | `save_workspace`, `load_workspace`, `list_workspaces` |
| `static/app/` | The **committed** built SPA assets served by the Python wheel; frontend CI rebuilds them and rejects a dirty asset diff | — |
| `../frontend/` | SPA source (Vite + React + TS + Lightweight Charts + TanStack Table/Virtual + cmdk; self-hosted fontsource Inter/JetBrains Mono variable); excluded from ruff/mypy/pytest. Local ritual for frontend changes: `npm run lint -- --deny-warnings && npm run test:coverage && npm run generate:api && npm run build && npm run test:e2e`; every step is a CI gate (install Playwright Chromium once per machine). | — |
| `../frontend/src/shell/` | The terminal shell (spec 2026-09-01 §4.2, Phase 3 T4–T7): `documents.ts` is the registry — one `DocumentDefinition` per profile `WindowId` (Chart · Research · Build · Strategy Performance Report · Compare · Forecast · Machine learning · Jobs & providers · Paper sessions · Governance) naming its `main`/`side` panes, plus the `DOCKS` (Market Watch, Navigator, Toolbox, Data Manager); `profiles.ts` holds the static crypto/equities manifests that decide which documents, docks and providers a profile shows (a display setting, never a permission, never sent to the server); `mdiModel.ts` keeps the open documents (`<window>` or `<window>:<instance>` keys); `menuModel.ts` lays out the eleven menus and `GROUP_MENU` must name every served CLI command group (`tests/unit/test_web_menu_groups.py`); `toolbarModel.ts`/`statusBarModel.ts` are pure; `App.tsx` composes title bar, `MenuBar`, `Toolbar` (with `ContextBar` as the symbol · venue · timeframe chip, the status chip and ⚖ Governance), the left docks, `DocumentArea` (only the active document mounts, so nothing polls behind a hidden tab), `Toolbox`, the right Data Manager dock and `StatusBar`; `PanelHost.tsx` still gives a panel its parameter handle without a layout engine. `screens.tsx` and `LibraryRail.tsx` are gone | `DOCUMENTS`, `documentOf`, `dockOf`, `panesByArea`, `PROFILES`, `showsWindow`, `openDocument`, `menuBar`, `GROUP_MENU`, `windowTitle`, `statusSegments`, `PanelHost` |
| `../frontend/src/components/FigureCard.tsx` · `../frontend/src/panels/FigureReport.tsx` | Every analytical chart is now a server-rendered figure. `svg.fonttype="path"` makes the figure's own text invisible to a screen reader, so the card carries the accessible copy in real HTML: alt text, the question it answers, its uncertainty and caveat, and a panel/unit/legend table | `FigureCard`, `FigureReport` |
| `../frontend/src/explain/` | The **explanation engine**: pure TS turning manifest numbers into dual-voice narratives (narrative/terse toggle) — gate stories, verdict band mirror (`bands.ts` ↔ `verdict.py`, drift-guarded by vitest fixtures of real manifests), rule-based next-step suggestions, metric glossary | `gateStories`, `verdictStories`, `suggestions`, `GLOSSARY`, `recomputeVerdict` |
| `../frontend/src/components/ChartAnnotationPrimitive.ts` | One Lightweight Charts series primitive for deterministic line/zone/polyline/swing anchors; avoids one chart series per annotation | `ChartAnnotationPrimitive` |
| `../frontend/src/panels/{runBrowserModel,jobProgress,v3Models,mlTearsheetModel,researchCockpitModel}.ts` | Pure typed panel models for capability-scoped run selection, honest job timing/ETA, v3/research lifecycle state, and ML diagnostics; colocated Vitest files own branch semantics | bounded render-state projections only; no metric or verdict composition |
| `../frontend/src/panels/ProviderSystem.tsx` · `PaperMonitor.tsx` · `JobMonitor.tsx` | Provider/system readiness, durable sandbox monitoring, and running-first job cards with exact elapsed/current-operation/output state, accessible progress, evidence-bounded ETA, live logs, and known-job cancel | `ProviderSystem`, `PaperMonitor`, `JobMonitor` |
| `../frontend/src/panels/ResearchCockpit.tsx` | Generated-contract client for the safe Research Case operations plus the ADR-0021 read plane; sticky case header, Python-authoritative HypothesisCard/scorecard, linked-context follow, New Idea focus, the R6e Decision tab, and the S5c existing Study tab for the exact server-masked semantic read plus `ResearchStudyStatusV1` owner-event/D1 guidance | read-only study projection; no source-pack screening, approval, D1 launch, owner decision, D2, promotion, paper, broker, or order authority |
| `../frontend/src/panels/{ResearchBacklog,EvidenceHub,ResearchDataExplorer,CodexBench}.tsx` · `researchBacklogModel.ts` · `researchChipModel.ts` | Research-first views embedded in the fixed Explore/Build screens: bounded backlog, evidence hub, registered data inventory, and context-packet collaboration; all scorecard and checklist semantics come from Python projections | read-only; New Idea capture asks for zero trading-rule inputs |
| `../frontend/src/panels/DataManager.tsx` · `dataManagerModel.ts` · `../frontend/src/state/settings.ts` (`profile`) | The one Data Manager tab on the Research screen (replaced the Market Data and Research Data tabs on 2026-09-02): pull form with native date inputs and the profile's defaults/watchlist (crypto → `XRP/USDT` · ccxt · Binance; equities → `AAPL` · Tiingo), first-bar Estimate (`GET /api/data/first-bar`) with a `listed <date> · ~N bars` hint and a one-click `Start there` retry taken from the listing or from the failed job's own CLI message, stored pairs, Expansion SSD datasets from `crypto-data coverage/storage` with the unmounted state shown honestly (never a cached ready), and the reviewed-asset CLI recipe with no mutation; its second tab embeds the governed `ResearchDataExplorer`/Crypto Data Center unchanged. `dataManagerModel.ts` is pure (`pullDefaults`, `starterSymbols`, `validateDates`, `storageRow`, `listingHint`, `retryStartFrom`); `profile: 'crypto' \| 'equities'` persists in `alpha.settings`, mirrors to `html[data-profile]`, and changes data defaults only | read-only projections; `buildDataPullArgs` throws the date problem the CLI would reject |
| `_invoke.py` (`Job.failure_reason`) · `JobMonitor.tsx` failed rows | A failed job's terminal error and current step are the CLI's own `Error:` line (`TYPER_USE_RICH=0` in the job environment; box glyphs never become a step), and `_catalog._run_json` raises that same line instead of Click's usage banner so every relaying router's 4xx carries the plain message | `Job.failure_reason`, `_current_step` |
| `_runs.py` (`display_name`) · `../frontend/src/panels/Navigator.tsx` | Every run projection (`/api/runs` items, run detail, activity events) carries a server-computed additive `display_name` — `<strategy> D1 — <symbol> · <source> · <first → last curve date> · run <8-hex>` — omitting whatever the manifest does not record; the Navigator dock shows it as the backtest leaf and opens the report document titled by it (the Library rail that first showed it on 2026-09-02, Phase 2 C1, is gone since Phase 3) | `display_name`, `_curve_period` |
| `../frontend/src/panels/rundetail/index.tsx` · `reportModel.ts` · `FigureReport.tsx` (`FigureSection`, `RunFigures`) | The Strategy Performance Report (Phase 2 C2): one window titled by `display_name` with a left `role=tree` (Strategy Analysis · Trade Analysis · Periodical Analysis · Robustness · Settings & data) mapped 1:1 onto the figure catalogue sections, gates, trades, stress and artifacts by the pure `reportTree`; empty leaves stay visible with their reason; `summaryRows` is a key/value table of recorded manifest values only (never client arithmetic; `not recorded` otherwise); the outcome band, tabs and both watermark banners collapsed into one `.rg-watermark-chip` whose title carries the full server sentence. Evidence Hub keeps a flat `RunFigures` list | `reportTree`, `summaryRows`, `watermarkChip` |
| `../frontend/src/components/FigureOverlay.tsx` · `figureExport.ts` · `FigureCard.tsx` (`Expand`, double-click) | Figure maximise (Phase 2 C3): a focus-trapped `role=dialog` overlay with Save PNG / Save SVG (existing image endpoints, `<run8>-<figure>.<ext>`), Copy (fetches the PNG and hands the blob to the Clipboard API; disabled with the reason when unavailable) and Close; Esc restores focus to the card. Question/uncertainty/caveat prose stays in the DOM in both explain modes and is visually shown only in narrative (Notes) mode | `exportNames`, `copyCapability`, `notesVisible` |
| `../frontend/src/panels/Governance.tsx` · `governanceModel.ts` · `useSelectedRunWatermark.ts` · `shell/Toolbar.tsx` (⚖ Governance button) | The Governance document (Phase 2 C4 as a dialog, a document since Phase 3 T7a; opened from the toolbar button, the status chip and the View menu): seven pages composed from existing reads only — Authority & status (the five former hazard-stripe sentences verbatim, paper flags, activity stream, selected-run watermark), Touch ID, Research gates (linked project lock + `Open research case` deep link), Overrides (verbatim), Providers, Storage (`storageRow`), Glossary (profile-tagged entries filtered by `glossaryEntries(profile)`). The `.sandbox-banner` stripes, the Development Center lock notice and the Glossary foot are gone from working documents; Build affordance locks are unchanged; nothing derives authority in the browser | `GOVERNANCE_SENTENCES`, `governancePages`, `glossaryEntries` |
| `../frontend/src/shell/statusModel.ts` · `shell/Toolbar.tsx` (`StatusCluster`) | One toolbar status chip (Phase 2 C5): the selected run's research-gate watermark verbatim → `Research gate open` for the linked project → `Paper only`; it opens the Governance document. The watermark is asserted on three surfaces (report chip, toolbar chip, Governance row) — proven sequentially since Phase 3 because only the active document mounts. At ≤1280px the toolbar's context window hides so no toolbar control is obscured (axe target-size) | `statusChip` |
| `../frontend/src/panels/JobMonitor.tsx` · `jobTableModel.ts` | One dense `.blotter` jobs table (Phase 2 C6): status · command · started · elapsed · ETA · progress · now · actions; `jobRows` owns running-first ordering and every cell string, relaying the CLI failure message exactly as the `now` cell and its `title`; the progressbar keeps its accessible name; the console expands beneath the row | `jobRows` |
| `../frontend/src/panels/V3Workbenches.tsx` · `KronosStudio.tsx` | 15 exposed stage IDs (13 core lifecycle + Kronos/ML tracks), immutable setup/AgentBrief/Findings, required Monte Carlo launch/read surfaces, and complete-sample Kronos K-lines and calibration | professional v3 workstation panels |
| `../frontend/src/panels/V3Workbenches.tsx` project-workspace extension | Existing Development Center renders the verified hash-only project workspace and generated-reference refresh | authority remains `none`; no execution action |
| `../frontend/src/panels/ChartDataAlternative.tsx` | The price chart is a canvas, so a screen reader sees an empty box. This is the same bars as real markup, paginated, with an exact CSV of what was returned — it is why the chart is allowed to be a canvas | `ChartDataAlternative` |

Server = thin JSON+SSE orchestrator; all composition stays behind `alpha`. Actions plus provider/system and engine-backed projections subprocess `alpha`; research reads come from artifacts, and paper reads from the public operational journal. The real conversational path is `alpha_mcp`, not an in-app LLM (the AI Console panel points to it). OpenAPI, generated TypeScript, frontend coverage/lint/build, and committed `static/app` freshness are mandatory CI gates.
